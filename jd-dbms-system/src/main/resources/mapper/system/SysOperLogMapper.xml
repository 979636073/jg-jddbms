<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jd.system.mapper.SysOperLogMapper">

    <resultMap type="com.jd.system.domain.SysOperLog" id="SysOperLogResult">
        <id property="operId" column="OPER_ID"/>
        <result property="title" column="TITLE"/>
        <result property="businessType" column="BUSINESS_TYPE"/>
        <result property="method" column="METHOD"/>
        <result property="requestMethod" column="REQUEST_METHOD"/>
        <result property="operatorType" column="OPERATOR_TYPE"/>
        <result property="operName" column="OPER_NAME"/>
        <result property="deptName" column="DEPT_NAME"/>
        <result property="operUrl" column="OPER_URL"/>
        <result property="operIp" column="OPER_IP"/>
        <result property="operLocation" column="OPER_LOCATION"/>
        <result property="operParam" column="OPER_PARAM"/>
        <result property="jsonResult" column="JSON_RESULT"/>
        <result property="status" column="STATUS"/>
        <result property="errorMsg" column="ERROR_MSG"/>
        <result property="operTime" column="OPER_TIME"/>
        <result property="costTime" column="COST_TIME"/>
    </resultMap>

    <sql id="selectOperLogVo">
        select OPER_ID,
               TITLE,
               BUSINESS_TYPE,
               METHOD,
               REQUEST_METHOD,
               OPERATOR_TYPE,
               OPER_NAME,
               DEPT_NAME,
               OPER_URL,
               OPER_IP,
               OPER_LOCATION,
               OPER_PARAM,
               JSON_RESULT,
               STATUS,
               ERROR_MSG,
               OPER_TIME,
               COST_TIME
        from SYS_OPER_LOG
    </sql>

    <insert id="insertOperlog" parameterType="com.jd.system.domain.SysOperLog">
        insert into SYS_OPER_LOG(TITLE, BUSINESS_TYPE, METHOD, REQUEST_METHOD, OPERATOR_TYPE, OPER_NAME,
                                  DEPT_NAME,
                                  OPER_URL, OPER_IP, OPER_LOCATION, OPER_PARAM, JSON_RESULT, STATUS, ERROR_MSG,
                                  COST_TIME, OPER_TIME)
        values (#{title}, #{businessType}, #{method}, #{requestMethod},
                #{operatorType}, #{operName}, #{deptName},
                #{operUrl}, #{operIp}, #{operLocation}, #{operParam}, #{jsonResult}, #{status}, #{errorMsg},
                #{costTime}, CURRENT_TIMESTAMP)
    </insert>

    <select id="selectOperLogList" parameterType="com.jd.system.domain.SysOperLog" resultMap="SysOperLogResult">
        <include refid="selectOperLogVo"/>
        <where>
            <if test="operIp != null and operIp != ''">
                AND OPER_IP like concat('%', #{operIp}, '%')
            </if>
            <if test="title != null and title != ''">
                AND TITLE like concat('%', #{title}, '%')
            </if>
            <if test="businessType != null">
                AND BUSINESS_TYPE = #{businessType}
            </if>
            <if test="businessTypes != null and businessTypes.length > 0">
                AND BUSINESS_TYPE in
                <foreach collection="businessTypes" item="businessType" open="(" separator="," close=")">
                    #{businessType}
                </foreach>
            </if>
            <if test="status != null">
                AND STATUS = #{status}
            </if>
            <if test="operName != null and operName != ''">
                AND OPER_NAME like concat('%', #{operName}, '%')
            </if>
            <if test="params.beginTime != null and params.beginTime != ''"><!-- 开始时间检索 -->
                AND OPER_TIME &gt;= #{params.beginTime}
            </if>
            <if test="params.endTime != null and params.endTime != ''"><!-- 结束时间检索 -->
                AND OPER_TIME &lt;= #{params.endTime}
            </if>
        </where>
        order by OPER_ID desc
    </select>

    <delete id="deleteOperLogByIds" parameterType="Long">
        delete from SYS_OPER_LOG where oper_id in
        <foreach collection="array" item="operId" open="(" separator="," close=")">
            #{operId}
        </foreach>
    </delete>

    <select id="selectOperLogById" parameterType="Long" resultMap="SysOperLogResult">
        <include refid="selectOperLogVo"/>
        where OPER_ID = #{operId}
    </select>

    <update id="cleanOperLog">
        truncate table SYS_OPER_LOG
    </update>

</mapper>
