<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jd.system.mapper.SysNoticeMapper">

    <resultMap type="com.jd.system.domain.SysNotice" id="SysNoticeResult">
        <result property="noticeId"       column="NOTICE_ID"       />
        <result property="noticeTitle"    column="NOTICE_TITLE"    />
        <result property="noticeType"     column="NOTICE_TYPE"     />
        <result property="noticeContent"  column="NOTICE_CONTENT"  />
        <result property="status"         column="STATUS"          />
        <result property="createBy"       column="CREATE_BY"       />
        <result property="createTime"     column="CREATE_TIME"     />
        <result property="updateBy"       column="UPDATE_BY"       />
        <result property="updateTime"     column="UPDATE_TIME"     />
        <result property="remark"         column="REMARK"          />
    </resultMap>

    <sql id="selectNoticeVo">
        select NOTICE_ID, NOTICE_TITLE, NOTICE_TYPE, NOTICE_CONTENT, STATUS, CREATE_BY, CREATE_TIME, UPDATE_BY, UPDATE_TIME, REMARK
		from SYS_NOTICE
    </sql>

    <select id="selectNoticeById" parameterType="Long" resultMap="SysNoticeResult">
        <include refid="selectNoticeVo"/>
        where NOTICE_ID = #{noticeId}
    </select>

    <select id="selectNoticeList" parameterType="com.jd.system.domain.SysNotice" resultMap="SysNoticeResult">
        <include refid="selectNoticeVo"/>
        <where>
			<if test="noticeTitle != null and noticeTitle != ''">
				AND NOTICE_TITLE like concat('%', #{noticeTitle}, '%')
			</if>
			<if test="noticeType != null and noticeType != ''">
				AND NOTICE_TYPE = #{noticeType}
			</if>
			<if test="createBy != null and createBy != ''">
				AND CREATE_BY like concat('%', #{createBy}, '%')
			</if>
		</where>
    </select>

    <insert id="insertNotice" parameterType="com.jd.system.domain.SysNotice">
        insert into SYS_NOTICE (
			<if test="noticeTitle != null and noticeTitle != '' ">NOTICE_TITLE, </if>
			<if test="noticeType != null and noticeType != '' ">NOTICE_TYPE, </if>
			<if test="noticeContent != null and noticeContent != '' ">NOTICE_CONTENT, </if>
			<if test="status != null and status != '' ">STATUS, </if>
			<if test="remark != null and remark != ''">REMARK,</if>
 			<if test="createBy != null and createBy != ''">CREATE_BY,</if>
 			CREATE_TIME
 		)values(
			<if test="noticeTitle != null and noticeTitle != ''">#{noticeTitle}, </if>
			<if test="noticeType != null and noticeType != ''">#{noticeType}, </if>
			<if test="noticeContent != null and noticeContent != ''">#{noticeContent}, </if>
			<if test="status != null and status != ''">#{status}, </if>
			<if test="remark != null and remark != ''">#{remark},</if>
 			<if test="createBy != null and createBy != ''">#{createBy},</if>
 			CURRENT_TIMESTAMP
		)
    </insert>

    <update id="updateNotice" parameterType="com.jd.system.domain.SysNotice">
        update SYS_NOTICE
        <set>
            <if test="noticeTitle != null and noticeTitle != ''">NOTICE_TITLE = #{noticeTitle}, </if>
            <if test="noticeType != null and noticeType != ''">NOTICE_TYPE = #{noticeType}, </if>
            <if test="noticeContent != null">NOTICE_CONTENT = #{noticeContent}, </if>
            <if test="status != null and status != ''">STATUS = #{status}, </if>
            <if test="updateBy != null and updateBy != ''">UPDATE_BY = #{updateBy},</if>
 			UPDATE_TIME = CURRENT_TIMESTAMP
        </set>
        where NOTICE_ID = #{noticeId}
    </update>

    <delete id="deleteNoticeById" parameterType="Long">
        delete from SYS_NOTICE where NOTICE_ID = #{noticeId}
    </delete>

    <delete id="deleteNoticeByIds" parameterType="Long">
        delete from SYS_NOTICE where NOTICE_ID in
        <foreach item="noticeId" collection="array" open="(" separator="," close=")">
            #{noticeId}
        </foreach>
    </delete>

</mapper>
